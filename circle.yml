general:
machine:
  environment:
    USER_NAME: hisasann
    USER_EMAIL: hisasann@gmail.com
  ruby:
    version: 2.5.1
dependencies:
    override:
      - bundle install
test:
  override:
    - bundle exec jekyll build --trace
